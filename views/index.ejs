<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>URL Shortener</title>
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <style>
      :root {
        --primary: #6a1b9a;
        --secondary: #fbc02d;
        --dark: #3e0d1e;
        --light: #f5f5f5;
        --accent: #9c27b0;
      }

      * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
        font-family: "Poppins", sans-serif;
      }

      body {
        background-color: var(--dark);
        color: var(--light);
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        padding: 20px;
        text-align: center;
      }

      .hero {
        max-width: 800px;
        margin: 0 auto;
        padding: 40px 20px;
        animation: fadeIn 0.8s ease;
      }

      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: translateY(20px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      .logo {
        margin-bottom: 30px;
      }

      .logo h1 {
        color: var(--secondary);
        background-color: var(--primary);
        padding: 15px 30px;
        border-radius: 8px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        font-size: 2.5rem;
        letter-spacing: 1px;
        display: inline-block;
        margin-bottom: 20px;
      }

      .tagline {
        font-size: 1.5rem;
        margin-bottom: 40px;
        color: rgba(255, 255, 255, 0.9);
        line-height: 1.4;
      }

      .features {
        display: flex;
        justify-content: center;
        gap: 30px;
        margin-bottom: 50px;
      }

      .feature-card {
        background-color: rgba(255, 255, 255, 0.1);
        border-radius: 10px;
        padding: 25px;
        width: 250px;
        transition: all 0.3s ease;
      }

      .feature-card:hover {
        transform: translateY(-5px);
        background-color: rgba(255, 255, 255, 0.15);
      }

      .feature-icon {
        font-size: 2.5rem;
        margin-bottom: 15px;
        color: var(--secondary);
      }

      .feature-title {
        font-size: 1.2rem;
        font-weight: 600;
        margin-bottom: 10px;
      }

      .feature-desc {
        font-size: 0.9rem;
        color: rgba(255, 255, 255, 0.7);
      }

      .auth-buttons {
        display: flex;
        justify-content: center;
        gap: 20px;
        margin-top: 30px;
        flex-wrap: wrap;
      }

      .btn {
        background-color: var(--secondary);
        color: var(--dark);
        border: none;
        padding: 15px 40px;
        border-radius: 6px;
        font-size: 1.1rem;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.3s ease;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        text-decoration: none;
        display: inline-block;
      }

      .btn:hover {
        background-color: #ffd600;
        transform: translateY(-2px);
        box-shadow: 0 6px 8px rgba(0, 0, 0, 0.15);
      }

      .btn-outline {
        background-color: transparent;
        color: var(--secondary);
        border: 2px solid var(--secondary);
      }

      .btn-outline:hover {
        background-color: rgba(251, 192, 45, 0.1);
      }

      footer {
        margin-top: 60px;
        color: rgba(255, 255, 255, 0.6);
        font-size: 0.9rem;
      }

      @media (max-width: 768px) {
        .logo h1 {
          font-size: 1.8rem;
          padding: 12px 25px;
        }

        .tagline {
          font-size: 1.2rem;
        }

        .btn {
          padding: 12px 30px;
          font-size: 1rem;
          width: 100%;
        }

        .auth-buttons {
          flex-direction: column;
          gap: 15px;
          width: 100%;
          max-width: 300px;
          margin-left: auto;
          margin-right: auto;
        }
      }
    </style>
  </head>
  <body>
    <div class="hero">
      <div class="logo">
        <h1>URL Shortener</h1>
      </div>

      <p class="tagline">
        Create short, memorable links and track their performance with our
        powerful URL shortening service
      </p>

      <div class="features">
        <div class="feature-card">
          <div class="feature-icon">🔗</div>
          <h3 class="feature-title">Shorten Links</h3>
          <p class="feature-desc">
            Create short, easy-to-share URLs from long website links
          </p>
        </div>

        <div class="feature-card">
          <div class="feature-icon">📊</div>
          <h3 class="feature-title">Track Clicks</h3>
          <p class="feature-desc">
            Monitor how many people click on your shortened links
          </p>
        </div>

        <div class="feature-card">
          <div class="feature-icon">🔒</div>
          <h3 class="feature-title">Secure</h3>
          <p class="feature-desc">
            All links are encrypted and protected for your safety
          </p>
        </div>
      </div>

      <div class="auth-buttons">
        <a href="/user/login" class="btn">Login</a>
        <a href="/user/signup" class="btn btn-outline">Sign Up</a>
      </div>
    </div>

    <footer>&copy; 2025 URL Shortener. All rights reserved.</footer>
  </body>
</html>
